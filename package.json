{
  "name": "bossai-os-core",
  "version": "0.2.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "Open developer infrastructure extracted from BossAI OS: AI contracts, skills, workflows, local RAG, file parsing, and webhook security primitives.",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "demo": "node examples/governed-workflow.mjs",
    "test": "pnpm --filter @bossai/skill-engine test && pnpm --filter @bossai/workflow-engine test && pnpm --filter @bossai/file-parser test && pnpm --filter @bossai/local-rag test && pnpm --filter @bossai/webhook-security test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "verify:open-source-boundary": "node scripts/verify-open-source-boundary.mjs",
    "verify:publication": "node scripts/verify-publication-gate.mjs",
    "growth:traffic": "node scripts/github-traffic-report.mjs",
    "growth:traffic:check": "node scripts/github-traffic-report.mjs --self-test"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3"
  }
}
